export enum ButtonType {
  primary = 'primary',
  secondary = 'default',
  tertiary = 'link'
}

export enum ButtonSize {
  large = 'large',
  medium = 'default',
  small = 'small'
}

export enum TagType {
  closeable = 'closeable',
  default = 'default',
  checkable = 'checkable'
}
