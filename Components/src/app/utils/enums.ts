export enum ButtonType {
  primary = 'primary',
  secondary = 'default',
  tertiary = 'link'
}

export enum ButtonSize {
  large = 'large',
  medium = 'default',
  small = 'small'
}

export enum SideNavigationType {
  menu = 'menu',
  subMenu = 'subMenu',
  menuGroup = 'menuGroup',
  menuItem = 'menuItem'
}

export enum TagType {
  closeable = 'closeable',
  default = 'default',
  checkable = 'checkable'
}
