export enum ButtonType {
  primary = 'primary',
  secondary = 'default',
  tertiary = 'link',
  inverted = 'inverted'
}

export enum ButtonSize {
  large = 'large',
  medium = 'medium',
  small = 'small'
}

export enum SideNavigationType {
  menu = 'menu',
  subMenu = 'subMenu',
  menuGroup = 'menuGroup',
  menuItem = 'menuItem'
}

export enum TagType {
  closeable = 'closeable',
  default = 'default',
  checkable = 'checkable'
}

export enum ICardType {
  sms = 'sms',
  customer = 'customer',
  gift = 'giftManagement'
}
