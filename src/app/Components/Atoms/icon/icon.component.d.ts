import { OnInit } from '@angular/core';
export declare class IconComponent implements OnInit {
    toolTipTitle: string;
    name: string;
    color: string;
    size: number;
    constructor();
    ngOnInit(): void;
}
